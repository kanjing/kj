#include<stdio.h>

int main()
{
   int i,on;float a=2,b=1,sum=0;
   for(i=1;i<=20;i++)
       {
           sum+=a/b;
           on=a;
           a+=b;
           b=on;
      }
printf("sum=%f\n",sum);
}
